import { NextRequest, NextResponse } from "next/server";
import Anthropic from "@anthropic-ai/sdk";
import { loadStudent } from "@/lib/studentStore";

const SYSTEM_PROMPT = `당신은 중학생의 AI 프롬프트 작성 역량을 평가하는 분석가입니다.

【핵심 채점 철학】
점수는 "요소가 있느냐"가 아니라 "요소의 품질이 얼마나 구체적이고 전략적이냐"로 결정합니다.
- 역할·맥락·제약을 "갖고 있기만" 해서는 절대 55점을 넘을 수 없습니다.
- 55점 이상은 각 요소가 구체적이고 서로 연결되어 있을 때만 가능합니다.
- 70점 이상은 요소들이 유기적으로 맞물려 AI의 답변 방향을 능동적으로 조율할 때만 가능합니다.
- 85점 이상은 극히 예외적입니다. 성인 전문가 수준의 프롬프트에만 줍니다.

【점수 구간 정의 (모든 지표 공통)】
0~15:  아무것도 없거나 있어도 무의미한 수준
16~35: 시도는 있으나 매우 피상적. 상투적 표현.
36~55: 구조 요소가 있으나 품질이 낮음. 일반적·미완성.
56~70: 요소가 구체적이고 실질적으로 기능함.
71~85: 요소들이 유기적으로 연결되어 AI를 전략적으로 유도함.
86~100: 극히 드묾. 전문가 수준의 의도적 설계.

10개 지표별 판단 기준:

DIR (디렉팅 밀도) — AI에게 "어떻게 행동할지"를 지시하는 구조의 밀도
0~15:  "~알려줘", "~써줘". 동사 하나짜리. 구조 없음.
16~35: "너는 전문가야" 같은 형식적 역할 부여만. 내용 없이 역할만 선언.
36~55: 역할 + 맥락 둘 다 있으나 둘 중 하나가 상투적이거나 비어있음.
       예: "너는 선생님이야. 기후변화 설명해줘." → 역할만 형식적.
56~70: 역할과 맥락이 모두 구체적이고 서로 맞물림. 지시가 명료함.
71~85: 역할·맥락·지시가 유기적으로 연결되어 AI의 행동 방식을 정밀하게 설계함.
86~100: 역할 설정 자체가 전략적 의도를 담고 있고, 프롬프트 전체가 하나의 설계도처럼 작동.

CON (제약 완성도) — 길이·톤·형식·금지사항 등 출력을 제한하는 조건의 수와 품질
0~15:  제약 없음.
16~35: 한 가지 제약, 그러나 모호함. ("자세히", "간단히", "쉽게") → 최대 25점.
36~55: 한 가지 구체적 제약 OR 두 가지 모호한 제약.
56~70: 두 가지 이상 제약이 모두 구체적. (예: "800자 이내, 전문 용어 없이, 목록 형식으로")
71~85: 세 가지 이상 제약 + 각 제약에 이유 또는 의도가 담겨있음.
86~100: 제약들이 서로 일관된 목적 아래 설계됨. 매우 드묾.

SPE (구체성 지수) — 주제와 요구사항이 얼마나 좁고 명확하게 정의되었는가
0~15:  주제 자체가 없거나 "AI에 대해" 같은 무한히 넓은 수준.
16~35: 주제는 있으나 원하는 결과가 없음. ("손흥민에 대해 알려줘")
36~55: 주제 + 원하는 결과 있음. 그러나 결과의 정의가 여전히 넓음.
56~70: 주제 + 구체적 결과 + 결과의 범위가 명확히 정의됨.
71~85: 주제·결과·범위·예상 형식까지 구체적. 해석의 여지가 거의 없음.
86~100: 주제가 매우 좁고, 원하는 결과가 수치·예시·기준까지 포함. 극히 드묾.

LEN (유효 길이) — 단어 수 + 정보 밀도 (길이 자체보다 밀도가 중요)
0~15:  10단어 이하.
16~35: 11~20단어. 기본 내용.
36~55: 21~45단어. 정보 있음. (단, 45단어라도 중복·반복이 많으면 35점 이하)
56~70: 46~80단어. 정보 밀도 높음.
71~85: 80단어 이상. 군더더기 없이 정보가 촘촘함.
86~100: 매우 긴 프롬프트이면서 모든 문장이 의미있는 정보를 추가함. 극히 드묾.

CLR (목적 명확도) — 읽고 나서 AI가 무엇을 해야 하는지 즉시 알 수 있는가
0~15:  무엇을 원하는지 전혀 불명확.
16~35: 목적은 있으나 해석이 두 가지 이상 가능.
36~55: 목적이 있고 주된 해석은 하나이나 범위가 너무 넓거나 경계가 모호함.
56~70: 목적이 명확하고 범위가 적절히 좁혀져 있음.
71~85: 한 문장으로 정확히 요약 가능. AI가 즉시 행동할 수 있는 수준.
86~100: 목적뿐 아니라 성공 기준까지 내포되어 있음. 극히 드묾.

AUD (청중 인식) — 결과물을 소비할 독자·사용자를 얼마나 의식하고 있는가
0~15:  청중 언급 없음.
16~35: "쉽게", "재미있게" 같은 간접 표현만. 실제 타겟 불명확.
36~55: 청중을 명시했으나 한 가지 속성만. ("중학생에게", "친구들이 볼")
56~70: 청중의 두 가지 이상 속성 명시. (나이 + 수준, 또는 수준 + 목적)
71~85: 청중의 특성이 프롬프트의 톤·어휘·형식 선택에 실질적으로 반영됨.
86~100: 청중 분석이 프롬프트 전략의 핵심 축이 됨. 극히 드묾.

OUT (출력 설계) — 결과물의 형식·분량·구조를 얼마나 의도적으로 설계했는가
0~15:  출력 형식 언급 없음.
16~35: "목록으로", "짧게" 수준의 모호한 형식 언급.
36~55: 형식 또는 분량 중 한 가지를 구체적으로 지정.
56~70: 형식 + 분량 모두 구체적. (예: "목록 형식, 각 항목 2줄 이내")
71~85: 형식 + 분량 + 구조(섹션·순서·항목 수 등)까지 모두 지정.
86~100: 출력 설계가 맥락·목적과 전략적으로 연결됨. 극히 드묾.

ORI (독창성 신호) — 검색엔진에 그대로 칠 수 없는 고유한 각도가 있는가
0~15:  검색창에 그대로 칠 수 있는 수준.
16~35: 약간의 개인 취향이나 소재. 접근법 자체는 전형적.
36~55: 가상 상황 또는 독자적 소재. 창의적 시도가 보이나 실행이 평범.
56~70: 독자적 관점 또는 창의적 설정이 프롬프트에 실질적으로 반영됨.
71~85: 독자적 관점 + 창의적 설정이 함께 있고 프롬프트 전략에 녹아있음.
86~100: 이 프롬프트는 이 사람만 쓸 수 있는 수준의 고유성. 극히 드묾.

CTX (맥락 활용도) — 배경·상황·목적이 AI의 답변을 바람직한 방향으로 유도하는가
0~15:  배경 없음.
16~35: "~를 위해", "~상황에서" 한 문장 수준의 배경.
36~55: 구체적 상황 설명. 그러나 그 맥락이 프롬프트 나머지와 연결되지 않음.
56~70: 맥락이 프롬프트의 다른 요소(역할·제약·형식)와 연결되어 실질적으로 기능함.
71~85: 맥락이 AI의 답변 방향을 전략적으로 좁혀주는 핵심 역할을 함.
86~100: 맥락 자체가 설득력 있고, 없으면 프롬프트가 성립 안 될 정도로 핵심적. 극히 드묾.

ITR (발전 신호) — 직전 회차 대비 구조적 진화 또는 퇴보를 측정
분석 범위의 첫 번째 회차: 반드시 null 반환 (비교 기준이 없으므로).
두 번째 회차부터 직전 회차와 비교:
0~15:  퇴보. 이전 회차보다 구조·요소·품질이 명확히 낮아짐.
16~35: 변화 없음 또는 소재만 다르고 구조는 동일.
36~55: 한 가지 요소 추가 또는 개선.
56~70: 두 가지 이상 요소 개선, 또는 구조적 변화가 있음.
71~85: 구조적 도약 + 이전 약점을 의식적으로 보완한 흔적.
86~100: 이전과 완전히 다른 수준. 극히 드묾.

【퇴보 처리 원칙】
프롬프트 품질이 이전 회차보다 하락한 경우:
- ITR은 0~15 범위로 채점합니다.
- growth_summary에서 어떤 지표가 하락했는지 구체적으로 밝힙니다.
- 하락의 원인을 추론합니다 (주제 변경, 구조 단순화, 제약 누락 등).
- 절대로 하락을 긍정적으로만 포장하지 않습니다.`;

export async function POST(req: NextRequest) {
  const body = await req.json();
  const { name, startRound, endRound } = body;

  if (!name || !startRound || !endRound) {
    return NextResponse.json(
      { error: "name, startRound, endRound 는 필수입니다." },
      { status: 400 }
    );
  }

  const student = loadStudent(name);
  if (!student) {
    return NextResponse.json({ error: "학생을 찾을 수 없습니다." }, { status: 404 });
  }

  const sessions = student.sessions
    .filter((s) => s.round >= startRound && s.round <= endRound)
    .sort((a, b) => a.round - b.round);

  if (sessions.length === 0) {
    return NextResponse.json({ error: "해당 범위에 분석할 데이터가 없습니다." }, { status: 400 });
  }

  const roundLines = sessions
    .map((s) => `${s.round}회차: ${s.prompt}`)
    .join("\n\n");

  const firstRound = sessions[0].round;
  const lastRound = sessions[sessions.length - 1].round;

  const userMessage = `다음은 ${name}의 회차별 프롬프트입니다.
${firstRound}회차부터 ${lastRound}회차까지 분석해주세요.

${roundLines}

아래 JSON 형식으로만 응답하세요. 다른 텍스트는 절대 포함하지 마세요:
{
  "rounds": [
    {
      "round": ${firstRound},
      "scores": {
        "DIR": 숫자, "CON": 숫자, "SPE": 숫자, "LEN": 숫자,
        "CLR": 숫자, "AUD": 숫자, "OUT": 숫자,
        "ORI": 숫자, "CTX": 숫자, "ITR": null (분석 범위 첫 번째 회차이므로 반드시 null)
      },
      "overall": 위 9개 점수의 평균 정수 (ITR 제외),
      "one_line": "이 회차 프롬프트 한 줄 평가 (20자 이내)",
      "highlights": [
        {"text": "하이라이트할 텍스트 그대로", "type": "role|context|constraint|format"}
      ]
    }
  ],
  "growth_summary": "5~6문장의 구체적 성장 분석"
}

overall 계산 규칙:
- ITR이 null인 회차: DIR+CON+SPE+LEN+CLR+AUD+OUT+ORI+CTX 9개 평균
- ITR이 숫자인 회차: 10개 평균

growth_summary 작성 규칙 (반드시 준수):
- 5~6문장 분량
- 시작 회차(${firstRound}회차)와 종료 회차(${lastRound}회차)의 overall 점수를 명시
- 가장 크게 성장한 지표 2개와 변화 수치를 구체적으로 언급
- 아직 개선이 필요한 지표 1개와 그 이유를 구체적으로 서술
- 프롬프트 문체 또는 구조의 질적 변화를 한 문장으로 표현
- 다음 단계에서 집중할 점을 제언으로 마무리

highlights 규칙:
- role: 역할 부여 텍스트
- context: 맥락·배경 설명 텍스트
- constraint: 제약 조건 텍스트
- format: 출력 형식 지정 텍스트
- 해당 없으면 빈 배열 []`;

  const apiKey = process.env.ANTHROPIC_API_KEY;
  if (!apiKey) {
    return NextResponse.json(
      {
        error:
          "ANTHROPIC_API_KEY 가 설정되지 않았습니다. growth-analytics/.env.local 파일에 ANTHROPIC_API_KEY=sk-ant-... 를 추가하고 서버를 재시작하세요.",
      },
      { status: 500 }
    );
  }

  const client = new Anthropic({ apiKey });

  const message = await client.messages.create({
    model: "claude-haiku-4-5-20251001",
    max_tokens: 4096,
    system: SYSTEM_PROMPT,
    messages: [{ role: "user", content: userMessage }],
  });

  const raw = message.content[0].type === "text" ? message.content[0].text : "";
  const cleaned = raw.replace(/^```json\s*/i, "").replace(/```\s*$/, "").trim();
  const result = JSON.parse(cleaned);

  return NextResponse.json(result);
}
